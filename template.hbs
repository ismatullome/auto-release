## [{{version}}]({{compareLink}}) ({{releaseDate}})

{{#if features.length}}
### Features

{{#each features}}
* **{{this.title}}** ([{{this.commit}}]({{this.commitLink}}))
{{/each}}
{{/if}}

{{#if bugFixes.length}}
### Bug Fixes

{{#each bugFixes}}
* {{this.title}} ([{{this.commit}}]({{this.commitLink}}))
{{/each}}
{{/if}}
